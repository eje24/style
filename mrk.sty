\ProvidesPackage{mrk}
\RequirePackage{mrkmath}

\RequirePackage{kvoptions}

\newif\ifmrk@date \mrk@datetrue

\DeclareVoidOption{nodate}{\mrk@datefalse}

\ProcessKeyvalOptions*

\KOMAoptions{
	paper=letter,
	fontsize=12pt,
	usegeometry,
	numbers=noendperiod,
}

% SETUP
\author{Ben Bitdiddle}

\ifmrk@date\else\date{~\vspace{-5em}}\fi

% Stylize things
\RequirePackage{etoolbox} %patchcmd
% \addtokomafont{section}{\rmfamily} %controls KOMA header/section/subsection font family
\setkomafont{author}{\normalsize\scshape}
\setkomafont{date}{\normalsize\scshape}
\setkomafont{disposition}{\centering\scshape\scriptsize}
\setkomafont{title}{\scriptsize\scshape}
\patchcmd{\@maketitle}{\huge}{\small}{}{}
% \addtokomafont{section}{\rmfamily\centering\scshape}
\RedeclareSectionCommand[beforeskip=0.4\baselineskip]{paragraph}

% Page Margins
\RequirePackage{geometry}
\geometry{margin=2.0cm, includefoot}

% \usepackage{longtable} %tables spanning multiple pages
% \usepackage[shortlabels]{enumitem} %ALREADY ADDED
% \usepackage{mathtools} %ALREADY ADDED

% \usepackage[dvipsnames,hyperref]{xcolor} %ALREADY ADDED
% \usepackage[notref,notcite]{showkeys} %strange package - shows labels next to captions
% \usepackage{accents} % adds accents to letters
% \usepackage{url, hyperref} %ALREADY ADDED
% \usepackage{amsmath, amssymb, amsfonts, amsthm}
% \usepackage{mathrsfs} %fancy additional math fonts
% \usepackage{euscript} %more fancy additional math fonts -- possible leavev out
% \usepackage{tikz-cd} %commutitive diagrams
% \usepackage{enumitem}  %ALREADY ADDED

% \usepackage{graphicx} %ALREADY ADDED
% \usepackage{pst-node} %more commutative diagrams -- alternative to tikz-cd
% \usepackage{auto-pst-pdf} %pdf/DVI settings -- probably not needed
% \usepackage{natbib}


% Shortcuts: (\ensuremath would be great on basically all of these!)
% \newcommand\nc{\newcommand}
% \nc\nop\DeclareMathOperator
% \nc{\ad}{\text{ad }}
% \nc{\rr}{\mathbb{R}}
% \nc{\cc}{\mathbb{C}}
% \nc{\qq}{\mathbb{Q}}
% \nc{\zz}{\mathbb{Z}}
% \nc{\im}{\text{im}}
% \nc{\gr}{\text{gr}}
% \nec{\ff}{\mathbb{F}}
% \nc{\id}{\textbf{Id}}
% \nc{\mat}[4]{\begin{pmatrix}#1&#2\\#3&#4\end{pmatrix}}
% \nc{\vvec}[2]{\begin{pmatrix}#1\\#2\end{pmatrix}}
% \nc{\hvec}[2]{\begin{pmatrix}#1&#2\end{pmatrix}}

% \nc\mbb[1]{\mathbb{#1}}
% \nc\cal[1]{\mathcal{#1}}
% \nc\scr[1]{\EuScript{#1}}
% \nc\mf[1]{\mathfrak{#1}}
% \nc\msf[1]{\mathsf{#1}}
% \nc\into\hookrightarrow
% \nc\onto{\twoheadrightarrow}
% \nc\bs\backslash
% \nc\abs[1]{\lvert#1\rvert}
% \nc\norm[1]{\lVert#1\rVert}
% \nc\brac[1]{\langle#1\rangle}
% \nc\ol\overline
% \nc\ul\underline
% \nc\wh\widehat
% \nc\wt\widetilde
% \nc\diag{\mathrm{diag}}
% \nc\pos{\textnormal{pos}}

% \nc{\res}{\text{res}}
% \nc{\spec}{\text{Spec }}
% \nc{\proj}{\text{Proj }}
% \nc{\divv}{\text{div }}
% \nc{\gal}{\text{Gal}}
% \nc{\frob}{\text{Frob}}

% \newcommand{\ee}{\mathbb{E}}
% \newcommand{\pp}{\mathbb{P}}
% \newcommand{\var}{\text{Var}}
% \newcommand{\bin}[2]{\text{Bin}\left(#1,#2\right)}
% \newcommand{\bern}[1]{\text{Bern}\left(#1\right)}
% \newcommand{\expo}[1]{\text{Expo}\left(#1\right)}
% \newcommand{\unif}[2]{\text{Unif}\left(#1,#2\right)}
% \newcommand{\bet}[2]{\text{Beta}\left(#1,#2\right)}
% \newcommand{\pois}[1]{\text{Pois}\left(#1\right)}
% \newcommand{\gam}[2]{\text{Gamma}\left(#1,#2\right)}

 
